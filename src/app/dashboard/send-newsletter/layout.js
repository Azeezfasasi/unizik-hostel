export const metadata = {
  title: 'Send Newsletter | CANAN USA',
  description: 'Send newsletters to your members with ease using our intuitive interface.',
}

export default function SendNewsletterLayout({ children }) {
  return children
}